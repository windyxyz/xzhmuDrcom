# 变更日志

本文件记录 DrCom徐医的重要变化，格式参考 Keep a Changelog，版本遵循扩展 Manifest 版本。

## [Unreleased]

### 新增

- 设置页增加默认开启的自动同步、立即同步、整页重载和最近同步状态；本地/会话存储变化、页面聚焦与可见性恢复会按范围更新，页面可见时每 15 秒刷新连接状态。
- 增加默认关闭、仅本机保存且有 1 MiB/10 会话上限的门户诊断模式，支持脱敏 JSON 导出和确认后的清空；设置页与导出会显示暂停、淘汰计数和“记录可能不完整”提示。
- 现代在线页补齐已用时间、总量、上下行流量、余额、登录时间和网络详情，所有账号及地址字段在后台结构化脱敏；支持经典、完整、简化和隐藏四种显示模式。
- 现代登录页补齐学校运营商顺序、重置、自助服务、账号激活、找回密码和使用说明；外部业务继续由学校官方页面处理。

### 修复

- 设置页同步会保护未保存的账号与配置编辑；自动失败不反复弹出提示，整页重载在丢弃编辑前需要确认。
- 修复现代门户异步接管时的页面就绪识别、观察器清理与用户恢复原始页面后的终止状态；在线页面的下线继续仅由用户点击触发。
- 验证码页面不再被现代覆盖层遮挡；二维码和验证码场景安全降级到学校原页面。
- 注销前明确确认“注销并解绑 MAC”，取消时不调用后台，确认后继续使用解绑优先和完整注销回退流程。

### 文档

- 完善登录原理、函数调用链、DrCOM 请求顺序、参数来源、响应判定、状态写入和凭据安全边界，并在 README、安全策略与贡献指南中建立分层说明。

## [2.5.3] - 2026-08-31

### 新增

- 增加首次安装欢迎页、连接状态机、单通道登录、指数退避重试和后台重启恢复。
- 增加全局外观、自定义背景压缩、门户私有背景层和门户个性化设置入口。
- 增加统一危险操作确认对话框，覆盖删除账号、覆盖导入、恢复设置、清空记录和清除背景。
- 增加无外部依赖的确定性 ZIP 打包器、SHA-256 文件、分组测试和发布标签校验。
- 增加 GPL-3.0 许可证、贡献指南、安全策略以及 CI/标签发布工作流。

### 修复

- 账号自然键统一为“去除首尾空白的用户名 + 小写后缀”，保存和迁移时不再产生重复账号。
- 下线绑定本次实际认证身份；临时账号不会错误退出当前界面选中的其他账号。
- 下线成功后立即同步离线状态并取消重试；下线失败保留真实在线状态。
- 修复空历史网络字段下首次现代登录无法认证：登录前安全解析门户 URL 与 `v46ip`、`ss5`、`v4ip`、`ss3` 实时 IP，并真正使用有效的 `find_mac` 返回值；缺少 IP 时不发送密码。
- 登录前先检查 `/drcom/chkstatus`；已在线直接返回，`ret_code=2` 的“已在线”提示必须复核为在线才算成功，未知状态下保活不再发送凭据。
- 下线改为 `unbind_mac` 后条件式复核并在必要时回退完整 `Portal/logout`；只有明确离线才清除活动身份、重试和保活状态。
- DrCOM 响应采用结构化优先级解析，未知结果不再误报成功，诊断日志继续脱敏。
- 修复真实浏览器测试错过退出事件、无限等待或遗留 Chromium 进程的竞态。
- 修复设置图标越界、弹窗窄长、背景不立即落盘和多个窄屏布局问题。

### 安全

- 门户公开配置不再携带背景 Data URL；完整外观只通过经过验证的专用消息进入 closed Shadow DOM。
- 网页消息只接受扩展自身、顶层 frame、精确默认门户来源和仍位于门户的当前标签。
- schema 12 在成功迁移后删除旧顶层凭据，并清理历史无效账号/UI 字段。
- README、开发文档和安全策略补充本机明文保存、HTTP 门户、设备失陷及恶意扩展威胁边界。
- 所有界面、请求日志、诊断与导出约定默认隐藏密码和敏感查询参数。

### 重构

- 将后台经典 Service Worker 拆分为状态、DrCOM、账号、连接、门户和消息路由模块。
- 账号解析、自然键、标签和脱敏显示统一复用 `DrcomAccountUtils`。
- 删除账号 `note` 以及 `ui.subtitle`、`ui.density`、`ui.hideOriginalPortal` 死字段。

### 移除

- 在独有结论迁入开发指南和本变更日志后，删除已完成的审阅建议副本。
- 删除无版本且已被后续界面改动淘汰的两张 UI 审阅截图；真实浏览器布局测试继续作为可重复证据。

## [2.5.2] - 2026-08-01

- 修复认证结果误判、并发存储覆盖和前端异步错误反馈。

## [2.5.1] - 2026-07-01

- 为超过保存预算的自定义背景增加多轮高质量 WebP 压缩与降级缩放。

## [2.5.0] - 2026-06-01

- 引入多账号、现代门户、自动登录、保活、防跳转、脱敏请求记录和共享外观。
