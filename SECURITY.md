# 安全策略

## 支持范围

当前维护版本为 `2.5.3`。安全修复只基于当前源码分支进行；旧的手工副本或来源不明的扩展包不在支持范围内。

## 凭据威胁模型

本扩展为实现保存账号和自动登录，会把密码以明文保存在当前浏览器配置文件的扩展 `storage.local` 中。数据不会由项目主动上传，但 `storage.local` 不是密码保险库。

学校 DrCOM 门户和默认接口使用 HTTP，既有登录协议会把凭据放入 GET 查询参数。扩展无法为学校服务器增加 HTTPS；校园网链路、代理、调试器或抓包工具可能观察到认证请求。

项目通过可信上下文存储限制、严格的网页消息来源校验、最小门户消息接口和输出脱敏降低暴露面。这些措施无法抵御设备失陷、操作系统账号被控制、浏览器调试权限被滥用，或具有高权限的恶意扩展。

界面、日志和导出默认隐藏密码；请求日志还会清理账号、密码字段及敏感查询参数。这里的“隐藏”不代表底层已加密，保存的密码仍需供后台自动登录使用。

## 安全使用建议

- 只从可信源码或校验过 SHA-256 的发布包加载扩展。
- 不要在公共或他人管理的设备上保存账号。
- 发现设备失陷或可疑扩展后，先修改校园网密码，再删除浏览器中的已保存账号。
- 提交代码、测试数据、截图或诊断信息前，检查其中没有真实账号、密码、抓包 URL 和浏览器配置文件。

## 报告安全问题

请使用维护者提供的私密、非公开渠道报告安全问题；项目公开发布后，优先使用仓库的私密安全报告入口。不要在公开 Issue、聊天记录或附件中提供真实账号、密码、Cookie、完整认证 URL 或其他凭据。

报告可以包含受影响版本、最小复现步骤、预期影响和已经脱敏的日志。若暂时没有可用的私密渠道，请只报告“不含凭据”的问题摘要，等待维护者建立非公开联系后再补充细节。
