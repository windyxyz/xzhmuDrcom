# 安全策略

## 支持范围

当前维护版本为 `2.5.3`。安全修复只基于当前源码分支进行；旧的手工副本或来源不明的扩展包不在支持范围内。

## 凭据威胁模型

本扩展为实现保存账号和自动登录，会把密码以明文保存在当前浏览器配置文件的扩展 `storage.local` 中。数据不会由项目主动上传，但 `storage.local` 不是密码保险库。

学校 DrCOM 门户和默认接口使用 HTTP，既有登录协议会把凭据放入 GET 查询参数。扩展无法为学校服务器增加 HTTPS；校园网链路、代理、调试器或抓包工具可能观察到认证请求。

项目通过可信上下文存储限制、严格的网页消息来源校验、最小门户消息接口和输出脱敏降低暴露面。这些措施无法抵御设备失陷、操作系统账号被控制、浏览器调试权限被滥用，或具有高权限的恶意扩展。

界面、日志和导出默认隐藏密码；请求日志还会清理账号、密码字段及敏感查询参数。这里的“隐藏”不代表底层已加密，保存的密码仍需供后台自动登录使用。

## 认证请求数据流

页面不会直接请求 DrCOM。扩展先在后台检查 `/drcom/chkstatus`，再获取门户首页并静态解析当前 IP；可选的 `Portal/find_mac` 只携带账号标识、当前 IP 和协议字段。只有最终的 `Portal/login` 会读取密码，并把账号、密码与本次网络参数放入学校既有的 HTTP GET 查询参数。

如果状态已经明确在线，或者无法取得合法的当前/全局 IP，后台不会构造 `Portal/login`，因此不会发送密码。状态为 `unknown` 时，自动保活也不会尝试携带凭据登录。门户脚本传入的页面 URL 必须先通过顶层 frame、精确 origin、当前标签和动作白名单校验；门户正文不会被执行，也不会进入日志。

请求记录使用脱敏 URL，响应记录删除正文、完整账号、具体 IP/MAC 和凭据字段。但脱敏发生在扩展的日志与导出边界，不能消除 HTTP 传输、浏览器网络调试器、代理或已失陷设备观察请求的风险。函数调用顺序和字段来源见 [`docs/development-guide.md` 的“登录原理与调用链”](docs/development-guide.md#73-登录原理与调用链)。

## 门户诊断模式与导出

门户诊断模式默认关闭，且只接受默认 `http://10.10.10.2` 顶层门户页面的消息。启用后记录仅保存在本机 `chrome.storage.local.drcomPortalDiagnostics`，不会由扩展上传。隔离世界先使用共享清理器处理，后台在串行写入前再次清理；这两层脱敏都不应被视为加密或对已失陷设备的防护。

该模式明确排除输入值、凭据、Cookie、页面或扩展存储内容、完整账号、IP 和 MAC。导出的 JSON 以及用户另行保存的 MHTML 仍可能包含对环境有意义的脱敏结构或元数据，分享、上传或交给维护者前必须人工复核。不要把原始导出、MHTML、真实抓包、账号或密码暂存或提交到 Git，也不要在公共 Issue、聊天记录、文档或测试 fixture 中分享、引用或摘录。原始文件应存放在仓库外的私有位置；如确需临时置于仓库内，只能使用已由本机规则（例如 `.git/info/exclude`）忽略并经 `git check-ignore -q TEMP` 验证的 `TEMP/`。人工复核并进一步脱敏后，才可按需向可信维护者提供。

## 安全使用建议

- 只从可信源码或校验过 SHA-256 的发布包加载扩展。
- 不要在公共或他人管理的设备上保存账号。
- 发现设备失陷或可疑扩展后，先修改校园网密码，再删除浏览器中的已保存账号。
- 提交代码、测试数据、截图或诊断信息前，检查其中没有真实账号、密码、Cookie、原始抓包 URL、IP、MAC 或浏览器配置文件。

## 报告安全问题

请使用维护者提供的私密、非公开渠道报告安全问题；项目公开发布后，优先使用仓库的私密安全报告入口。不要在公开 Issue、聊天记录或附件中提供真实账号、密码、Cookie、完整认证 URL 或其他凭据。

报告可以包含受影响版本、最小复现步骤、预期影响和已经脱敏的日志。若暂时没有可用的私密渠道，请只报告“不含凭据”的问题摘要，等待维护者建立非公开联系后再补充细节。
